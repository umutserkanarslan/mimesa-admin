PUBLIC_SUPABASE_URL=https://your-project.supabase.co
PUBLIC_SUPABASE_ANON_KEY=your-anon-public-key

# Server-only — used for storage uploads when service-role bypass is needed
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Server-only — used for the auto-translate endpoint (TR → EN/AR)
OPENAI_API_KEY=sk-...
